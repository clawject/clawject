{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Clawject configuration schema",
  "additionalItems": false,
  "additionalProperties": false,
  "type": "object",
  "properties": {
    "unsafeTSVersion": {
      "type": "boolean",
      "default": false,
      "title": "Allows using TypeScript version not officially supported by this version of Clawject.",
      "examples": [
        true,
        false
      ]
    },
    "features": {
      "type": "object",
      "properties": {
        "keepContextNames": {
          "type": "boolean",
          "default": true,
          "title": "Keeps context class names in generated code, it's can be useful for debugging purposes",
          "examples": [
            true,
            false
          ]
        },
        "lazyBeans": {
          "type": "boolean",
          "default": false,
          "title": "Container will instantiate all singleton beans in context during initialization if set to `true` — beans will be initialized only when they are requested.",
          "examples": [
            true,
            false
          ]
        }
      },
      "additionalProperties": false
    }
  }
}
